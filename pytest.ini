[pytest]
python_files = test_*.py
addopts = --alluredir=allure-results
markers =
    admin: tests requiring admin account
    registration: tests that create new accounts
    booking: tests related to booking functionality